{
  "name": "grandpa-assistant-ui",
  "private": true,
  "version": "1.0.0",
  "description": "React and Electron desktop UI for Grandpa Assistant.",
  "author": "Hari Hara Sudhan",
  "type": "module",
  "scripts": {
    "icons:generate": "node scripts/generate-icons.mjs",
    "typescript:compat": "node scripts/ensure-typescript-compat.mjs",
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "electron": "electron .",
    "electron:dev": "electron .",
    "desktop:portable": "npm run icons:generate && npm run typescript:compat && npm run build && electron-builder --win portable",
    "desktop:installer": "npm run icons:generate && npm run typescript:compat && npm run build && electron-builder --win nsis",
    "desktop:build": "npm run icons:generate && npm run typescript:compat && npm run build && electron-builder --win portable nsis",
    "desktop:dir": "npm run icons:generate && npm run typescript:compat && npm run build && electron-builder --dir"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.3.4",
    "electron": "^31.7.7",
    "electron-builder": "^24.13.3",
    "png-to-ico": "^2.1.8",
    "sharp": "^0.33.5",
    "vite": "^5.4.10"
  },
  "main": "electron/main.cjs",
  "build": {
    "appId": "com.grandpaassistant.desktop",
    "productName": "Grandpa Assistant",
    "directories": {
      "output": "release"
    },
    "files": [
      "dist/**/*",
      "electron/**/*",
      "package.json"
    ],
    "extraResources": [
      {
        "from": "../runtime/artifacts/backend/dist/GrandpaAssistantBackend",
        "to": "backend/GrandpaAssistantBackend",
        "filter": [
          "**/*"
        ]
      }
    ],
    "win": {
      "icon": "assets/app-icon.ico",
      "signAndEditExecutable": false,
      "target": [
        {
          "target": "portable",
          "arch": [
            "x64"
          ]
        },
        {
          "target": "nsis",
          "arch": [
            "x64"
          ]
        }
      ]
    },
    "nsis": {
      "oneClick": true,
      "allowToChangeInstallationDirectory": false,
      "perMachine": false,
      "createDesktopShortcut": true,
      "createStartMenuShortcut": true,
      "runAfterFinish": true
    }
  }
}
